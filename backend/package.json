{
  "name": "resume-and-portfolio-maker-backend",
  "version": "1.0.0",
  "description": "Backend API for Resume and Portfolio Maker application",
  "main": "server.js",
  "scripts": {
    "start": "node server-final.js",
    "build": "cd ../frontend && npm install && npm run build && cp -r build ../backend/",
    "dev": "nodemon server-final.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "resume",
    "portfolio",
    "api",
    "mongodb",
    "express",
    "jwt"
  ],
  "author": "Resume Builder Team",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^21.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
